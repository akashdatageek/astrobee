# PROPOSED by `ent retrofit` — confidence: high
# target: hardware/fam_cmd_i2c/launch/entiendo.node.yaml
# note: owner, contract, and evals are stubs — review before accepting
# note: boundary-uncertain: no candidate fixture -> expected verdict yet; supply one before accepting (the law)
apiVersion: entiendo/v1
kind: Node
id: astrobee.hardware.fam_cmd_i2c.launch
name: launch
task: 'TODO: state in one sentence what astrobee.hardware.fam_cmd_i2c.launch is for
  (retrofit inferred this boundary)'
nodeKind: config
group: astrobee
owner: TODO
status: experimental
claims:
- hardware/fam_cmd_i2c/launch/fam_cmd_i2c.yaml
contract:
  invariants: []
  sideEffects: none
dependencies:
  calls: []
  reads: []
  writes: []
  config: []
evals:
  tier0:
  - type: invariant_check
observability:
  spanName: astrobee.hardware.fam_cmd_i2c.launch
approval:
  required: false
