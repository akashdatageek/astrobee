# PROPOSED by `ent retrofit` — confidence: high
# target: scripts/calibrate/config/entiendo.node.yaml
# note: owner, contract, and evals are stubs — review before accepting
# note: boundary-uncertain: no candidate fixture -> expected verdict yet; supply one before accepting (the law)
apiVersion: entiendo/v1
kind: Node
id: astrobee.scripts.calibrate.config
name: config
task: 'TODO: state in one sentence what astrobee.scripts.calibrate.config is for (retrofit
  inferred this boundary)'
nodeKind: config
group: astrobee
owner: TODO
status: experimental
claims:
- scripts/calibrate/config/april_grid.yaml
- scripts/calibrate/config/granite_april_tag.yaml
- scripts/calibrate/config/imu.yaml
- scripts/calibrate/config/iss_checkerboard.yaml
contract:
  invariants: []
  sideEffects: none
dependencies:
  calls: []
  reads: []
  writes: []
  config: []
evals:
  tier0:
  - type: invariant_check
observability:
  spanName: astrobee.scripts.calibrate.config
approval:
  required: false
