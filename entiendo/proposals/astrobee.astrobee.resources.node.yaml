# PROPOSED by `ent retrofit` — confidence: high
# target: astrobee/resources/entiendo.node.yaml
# note: owner, contract, and evals are stubs — review before accepting
# note: boundary-uncertain: no candidate fixture -> expected verdict yet; supply one before accepting (the law)
apiVersion: entiendo/v1
kind: Node
id: astrobee.astrobee.resources
name: resources
task: 'TODO: state in one sentence what astrobee.astrobee.resources is for (retrofit
  inferred this boundary)'
nodeKind: config
group: astrobee
owner: TODO
status: experimental
claims:
- astrobee/resources/diagnostics.yaml
contract:
  invariants: []
  sideEffects: none
dependencies:
  calls: []
  reads: []
  writes: []
  config: []
evals:
  tier0:
  - type: invariant_check
observability:
  spanName: astrobee.astrobee.resources
approval:
  required: false
